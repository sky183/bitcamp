StarTest3 에서 board[j]로 하면 length는 같은데 오류가 나는 이유

예제 6-14에서 Data 타입의 메서드 copy를 참조형으로 반환하는거면 굳이 tmp에 안넣어도 같은 값이 나오는데 왜 tmp에 넣는 것인지

다른 클래스에서 객체 배열을 참조하고자 할때 card.length 등

추상클래스에서 인스턴스를 생성할 수 없으면 어떻게 사용? - 오버라이딩해서 사용

public static class 클래스명() {}  <- 스태틱 클래스E

instanceOf에서는 참인데 왜 형변환은 가능한가?(조상에서 자손으로 형변환)

StackEx1에서 toString()을 오버라이드를 하지 않았는데 어떻게 값이 출력이 되나

조상의 참조변수로 자손의 객체를 가키리면 멤버를 사용할 수 잇는 범위가 줄어드는데 다형성으로 하는 이유?

E obj?? E가 무슨 타입? - 지네릭스 

IteratorEx1에서 왜 String으로 형변환하면 안되고 + "" 은 되는걸까 - toString을 호출한 셈이므로 

다른 클래스의 인스턴스를 사용하는 방법??
버튼 클릭시 액션이벤트가 발생한 버튼의 배열번호를 찾는 방법은??

링크드리스트 타입의 q와 큐 타입의 큐는 무슨 차이?

자바1000제 콘솔 예제에서 IO예외를 구현하고 이걸 어디에 던져서 어떻게 처리?

쓰레드 예제 19에서 예외를 발생시켜 쓰레드를 멈추느것?
인코딩 보는 방법??(텍스트 파일의 경우에만)
추상메서드를 구현한 내부에서 추상메서드를 왜 호출하는지// 책 870페이지

어떠한 경우에 클래스에 생성자가 없다고 오류가 나는건가? - 생성자를 직접 생성했을 경우
객체가 소멸되는 경우가 뭐지?

connect system  select sysdate from dual;